name: gbi_logistics
description: GBI Logistics - Conectando el Mundo
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  http: ^1.1.0
  intl: ^0.20.2
  cupertino_icons: ^1.0.5
  flutter_secure_storage: ^9.2.4
  animated_bottom_navigation_bar: ^1.3.0
  animate_do: ^4.2.0
  shared_preferences: ^2.2.0
  audioplayers: ^6.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/sounds/
